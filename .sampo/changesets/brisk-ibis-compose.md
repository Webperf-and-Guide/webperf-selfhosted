---
npm/@webperf/config: minor
---

Ship a versioned GHCR production Compose bundle with a source-build override, console-only loopback exposure, opt-in debug proxies, non-root read-only services, health checks, bounded resources, log rotation, and a sandboxed single-concurrency Lighthouse profile without default `SYS_ADMIN`.
